// ─── useTasks ─────────────────────────────────────────────────
// Re-exports the shared context hook so every page reads/writes the same task list.
// Replace with real API calls in the final phase.

export { useTasks } from "../context/TasksContext";
